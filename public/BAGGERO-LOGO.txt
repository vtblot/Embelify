Official Baggero logos — drop YOUR real PNGs here (never regenerate):

  public/BAGGERO + LOGO fond blanc.png   ← footer, fond clair
  public/BAGGERO + LOGO.png              ← lockup fond sombre

The app reads these filenames as-is. Replace the files in place in Cursor’s
file explorer or commit + push to main.
